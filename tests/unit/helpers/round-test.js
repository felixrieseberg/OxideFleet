import {
  twoDecimal
} from '../../../helpers/two-decimal';
import { module, test } from 'qunit';

module('TwoDecimalHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = twoDecimal(42);
  assert.ok(result);
});
