import {
  prettyDate
} from '../../../helpers/pretty-date';
import { module, test } from 'qunit';

module('PrettyDateHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = prettyDate(42);
  assert.ok(result);
});
